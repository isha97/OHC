<template name="addMerchantAccountTemplate">
    {{#unless userLoggedIn}}
    <h3>You are unauthorized!!</h3>
    {{else}}
    {{#if isInRole 'siteadmin'}}
    <div class="container text-center">
        <div class="row">
            <div class="col-lg-2">
            </div>
            <div class="col-lg-8"><br>

                <span class="heading1-merchant">Add Account</span>

                <form>
                    <table class="center">
                        
                        <tr><td><input type="username" id="username" required placeholder="Username"></td></tr>
                        <tr><td><input type="password" id="password" required placeholder="Password"></td></tr>
                        <tr><td><input type="submit" value="Submit" class="btn-custom"></td></tr>
                    </table>
                </form>

            </div>
            <div class="col-lg-2">
            </div>
        </div>
    </div>
    {{else}}
    <h3>You are an unauthorized</h3>
    {{/if}}
    {{/unless}}
</template>
